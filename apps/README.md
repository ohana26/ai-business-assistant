# Apps

This directory contains deployable applications:

- `frontend/` - React + Vite + TypeScript client
- `backend/` - NestJS + TypeScript API

Scaffolding for both apps is added in the next implementation step.
