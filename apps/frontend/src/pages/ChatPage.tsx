import { PlaceholderCard } from "../components/common/PlaceholderCard";

export function ChatPage() {
  return (
    <PlaceholderCard
      title="Chat"
      description="The enterprise assistant interaction interface with citations and tool traces will live here."
    />
  );
}
