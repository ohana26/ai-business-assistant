import { PlaceholderCard } from "../components/common/PlaceholderCard";

export function SettingsPage() {
  return (
    <PlaceholderCard
      title="Settings"
      description="Workspace settings, provider configuration, and governance controls will be managed here."
    />
  );
}
