import { PlaceholderCard } from "../components/common/PlaceholderCard";

export function AssistantsPage() {
  return (
    <PlaceholderCard
      title="Assistants"
      description="Assistant configuration, model selection, and workspace assignment will be configured here."
    />
  );
}
