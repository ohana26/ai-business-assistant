{
  "name": "ai-business-assistant",
  "version": "0.1.0",
  "private": true,
  "description": "Enterprise AI Knowledge and Automation Platform monorepo",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "start": "npm run docker:up && concurrently -k -n backend,frontend -c yellow,cyan \"npm run start:dev -w backend\" \"npm run dev -w frontend\"",
    "dev": "concurrently -k -n backend,frontend -c yellow,cyan \"npm run start:dev -w backend\" \"npm run dev -w frontend\"",
    "stop": "npm run docker:down",
    "build": "echo \"Build scripts will be added after app scaffolding.\"",
    "test": "echo \"Test scripts will be added after app scaffolding.\"",
    "lint": "echo \"Lint scripts will be added after app scaffolding.\"",
    "docker:up": "docker compose -f infrastructure/docker-compose.yml up -d",
    "docker:down": "docker compose -f infrastructure/docker-compose.yml down",
    "docker:logs": "docker compose -f infrastructure/docker-compose.yml logs -f"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
