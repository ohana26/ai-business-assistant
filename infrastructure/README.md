# Infrastructure

Infrastructure assets for local and future cloud environments.

Upcoming items:
- `docker-compose.yml` for PostgreSQL (pgvector) and Ollama
- environment and deployment configuration
